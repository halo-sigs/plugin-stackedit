name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  ci:
    uses: halo-sigs/reusable-workflows/.github/workflows/plugin-ci.yaml@v3
    permissions:
      contents: read
      packages: read
    with:
      node-version: "20"
      pnpm-version: "10"
